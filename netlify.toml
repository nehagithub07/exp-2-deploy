# Netlify configuration for the static multi-page lab site.

[build]
  # All HTML, CSS, JS, and assets live in the repo root; no build step required.
  publish = "."

# Redirect the site root to the main landing page so hitting "/" works.
[[redirects]]
  from = "/"
  to = "/aim.html"
  status = 301
