# Netlify configuration for the static multi-page lab site.

[build]
  publish = "."

# Serve aim.html as the site root (no redirect in the browser bar)
[[redirects]]
  from = "/"
  to = "/aim.html"
  status = 200
